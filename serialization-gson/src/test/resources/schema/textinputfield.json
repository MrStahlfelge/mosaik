{
  "title": "TextInputField",
  "description": "Text field to enter general text",
  "type": "object",
  "properties": {
    "type": {
      "const": "TextInputField"
    },
    "id": {
      "description": "The unique identifier for the element. Optional.",
      "type": "string"
    },
    "onLongPress": {
      "description": "Action to be started when long press is performed",
      "$ref": "action.json"
    },
    "onClick": {
      "description": "Action to be started when click or tap is performed",
      "$ref": "action.json"
    },
    "visible": {
      "description": "Visibility of this object. Optional, defaults to true",
      "type": "boolean"
    },
    "error": {
      "description": "Shows an error message on the text field if not null",
      "type": "string"
    },
    "value": {
      "type": "string"
    },
    "placeholder": {
      "type": "string"
    },
    "enabled": {
      "type": "boolean"
    },
    "endIcon": {
      "$ref": "icontype.json"
    },
    "onEndIconClicked": {
      "$ref": "action.json"
    },
    "onValueChanged": {
      "$ref": "action.json"
    }
  },
  "required": [
    "type"
  ]
}