{
  "title": "WalletChooseButton",
  "description": "Button type element to let the user choose one of his wallets",
  "type": "object",
  "properties": {
    "type": {
      "const": "WalletChooseButton"
    },
    "onValueChangedAction": {
      "description": "Action to be started when value was changed",
      "type": "string"
    },
    "address": {
      "description": "Initial address set",
      "type": "string"
    },
    "enabled": {
      "type": "boolean"
    },
    "mandatory": {
      "type": "boolean"
    },
    "visible": {
      "description": "Visibility of this object. Optional, defaults to true",
      "type": "boolean"
    }
  },
  "required": [
    "type"
  ]
}