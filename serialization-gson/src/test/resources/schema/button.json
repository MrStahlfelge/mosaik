{
  "title": "Button",
  "description": "Button for the user to click",
  "type": "object",
  "properties": {
    "type": {
      "const": "Button"
    },
    "id": {
      "description": "The unique identifier for the element. Optional.",
      "type": "string"
    },
    "onLongPress": {
      "description": "Action to be started when long press is performed",
      "type": "string"
    },
    "onClick": {
      "description": "Action to be started when click or tap is performed",
      "type": "string"
    },
    "visible": {
      "description": "Visibility of this object. Optional, defaults to true",
      "type": "boolean"
    },
    "text": {
      "description": "Button text",
      "type": "string"
    },
    "maxLines": {
      "type": "integer"
    },
    "truncationType": {
      "$ref": "truncationtype.json"
    },
    "textAlignment": {
      "$ref": "halignment.json"
    },
    "style": {
      "$ref": "buttonstyle.json"
    }
  },
  "required": [
    "type"
  ]
}